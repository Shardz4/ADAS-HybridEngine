[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "adas_pilot"
version = "0.1.0"
requires-python = ">=3.9"
readme = "README.md"
description = "ADAS lane detection Rust extension"

[tool.maturin]
features = ["pyo3/extension-module"]